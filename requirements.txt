# 核心 LLM 调用
openai>=1.0.0

# 配置与环境管理
python-dotenv>=1.0.0
pyyaml>=6.0

# 数据与科学计算
numpy>=1.24.0
pandas>=2.0.0
xarray>=2023.0.0
netCDF4>=1.6.0
scipy>=1.10.0

# 可视化
matplotlib>=3.7.0
seaborn>=0.12.0
# cartopy>=0.21.0        # 地图投影（可选）
# plotly>=5.0.0          # 交互式图表（可选）

# 气候数据获取
cdsapi>=0.5.0
eccodes>=1.6.0           # GRIB 格式（可选）

# 记忆模块（向量数据库）
chromadb>=0.4.0

# 测试与基准
pytest>=7.0.0

# Jupyter Notebook（可选）
jupyter>=1.0.0
ipykernel>=6.0.0

# 其他工具
tqdm>=4.65.0
# pymannkendall>=1.4.0   # Mann-Kendall 检验（可选）